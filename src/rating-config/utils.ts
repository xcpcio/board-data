export function generateID(startYear: number, endYear: number) {
    return `Asia-East-CPC-${startYear}-${endYear}`;
}

export function generateName(startYear: number, endYear: number) {
    return `Rating for the ${startYear} - ${endYear} Season Asia East Collegiate Programming Contest`;
}
